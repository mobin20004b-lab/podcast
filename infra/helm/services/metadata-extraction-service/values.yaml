nameOverride: metadata-extraction-service
fullnameOverride: metadata-extraction-service

image:
  repository: ghcr.io/podcast/metadata-extraction-service
  tag: 0.1.0

service:
  port: 80
  targetPort: 8080

containerPort: 8080

env:
  SERVICE_NAME: metadata-extraction-service
  LOG_LEVEL: info
  OTEL_SERVICE_NAME: metadata-extraction-service
