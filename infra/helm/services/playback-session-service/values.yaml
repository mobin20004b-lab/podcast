nameOverride: playback-session-service
fullnameOverride: playback-session-service

image:
  repository: ghcr.io/podcast/playback-session-service
  tag: 0.1.0

service:
  port: 80
  targetPort: 8080

containerPort: 8080

env:
  SERVICE_NAME: playback-session-service
  LOG_LEVEL: info
  OTEL_SERVICE_NAME: playback-session-service
