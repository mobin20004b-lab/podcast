apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: podcast-platform-prod

nameSuffix: -prod

labels:
  - pairs:
      environment: prod
      workload-tier: baseline
