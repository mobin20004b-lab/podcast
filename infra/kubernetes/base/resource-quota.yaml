apiVersion: v1
kind: ResourceQuota
metadata:
  name: podcast-platform-quota
  namespace: podcast-platform
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "16"
    limits.memory: 32Gi
    pods: "100"
